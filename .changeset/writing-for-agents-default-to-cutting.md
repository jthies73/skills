---
"mattpocock-skills": patch
---

Give `writing-for-agents` an explicit default on size: every edit aims to end shorter than it started, a new line earns its place only where the document cannot do its job without it, and adding one means finding one to remove. The Pruning section already carried the mechanisms (single source of truth, relevance, the no-op test) but never stated the stance they serve.
